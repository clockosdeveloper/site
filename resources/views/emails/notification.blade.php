<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{$title}}</title>
</head>
<body>
    <h1>{{$title}}</h1>
    <p>{{$content}}
        <br/><a href="{{$link}}) }}">{{trans('notification.view')}}</a></p>
</body>
</html>